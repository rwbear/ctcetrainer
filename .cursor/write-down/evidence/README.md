# Evidence

Drop screenshots / screen recordings referenced by write-down notes here.

Naming: `YYYY-MM-DD-<short-slug>.png` (or `.jpg` / `.webp` / `.mp4`)

This folder is gitignored. Link from board items as:
`evidence/YYYY-MM-DD-slug.png`
