# .cursor/write-down/

Legacy local helpers. **Live board is `planning/board.json`.**

| Path | Purpose |
|------|---------|
| `planning/board.json` | Source of truth (public, committed) |
| `planning/` | Visual folder browser on Pages |
| `.cursor/write-down/evidence/` | Optional local screenshots (gitignored) |
| `.cursor/skills/write-down/` | Agent workflow |

## How to use

In Cursor: **`write-down`** + note / screenshots.  
On phone/PC: open `/ctcetrainer/planning/` after deploy.
