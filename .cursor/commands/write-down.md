Run the write-down workflow for this project.

Follow `.cursor/skills/write-down/SKILL.md` exactly.

Source of truth: `planning/board.json`  
Visual board: `planning/` (GitHub Pages)

Arguments / notes from the user (if any):
$ARGUMENTS

If there are no arguments, show the current board summary from `planning/board.json`.

Hard rules:
- Investigate notes; dig into the app when needed.
- Inspect any attached images/videos fully before placing items.
- Place into the correct folder with default urgency null unless specified.
- Commit board.json after changes.
- Never mark Done without explicit user confirmation.
- Ask for clarification only when required.
- Light “candidate next” only; user locks the decision.
